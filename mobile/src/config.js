// ⚠️ SET THIS to your computer's LAN IP address.
//
// A physical phone running Expo Go CANNOT reach `localhost` — that points at the
// phone itself. Find your PC's IPv4 address with `ipconfig` (e.g. 192.168.1.10)
// and put it below. The phone and PC must be on the SAME Wi-Fi network.
//
// Example: 'http://192.168.1.10:5000/api/v1'
export const API_URL = 'http://192.168.1.10:5000/api/v1';
